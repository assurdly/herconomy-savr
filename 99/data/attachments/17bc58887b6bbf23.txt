try {
    const jsonData = pm.response.json();

    pm.test("Status code is 400", function () {
        pm.response.to.have.status(400);
    });

    pm.test("Status code response is Bad Request ", function () {
        pm.response.to.have.status(400);
    });

    pm.test("Response body has property 'message'", function () {
    pm.expect(jsonData).to.have.property("message");
    pm.expect(jsonData.message).to.be.an("array");
});

    pm.test('Verify a valid data error response it returned', () => {
        pm.expect(jsonData.message).eql(["Savr Employee Exists!"])
    })
} catch (errors) {
    console.error(errors)
}
