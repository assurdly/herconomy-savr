try {
const jsonData = pm.response.json();

pm.test("Response body property", function () {
    pm.expect(jsonData).to.be.an('object');
});
pm.test("Status code name has string", function () {
        pm.response.to.have.status("Bad Request");
    });
    pm.test("Verify a valid data error response it returned", function () {
        pm.expect(jsonData.email).to.eql(["This field may not be blank."]);
          pm.expect(jsonData.password).to.eql(["This field may not be blank."]);
    });
pm.test("Status code is 400", function () {
    pm.response.to.have.status(400);
});
} catch (error) {
    console.error(error)
}
