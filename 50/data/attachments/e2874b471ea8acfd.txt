try {
    const jsonData = pm.response.json();

    pm.test("Status code is 200", function () {
        pm.response.to.have.status(200);
    });

    pm.test("Response body property", function () {
        pm.expect(jsonData).to.be.an('object');
        pm.expect(jsonData.detail).to.be.an('array'); // Fixed the data type check
    });

    pm.test("Status code name has string", function () {
        pm.expect(pm.response.status).to.equal("OK"); // Changed to check the status text
    });
     pm.test('Verify a valid success response it returned', function () {
        pm.expect(jsonData.detail).to.eql(["Successful"]); // Updated the expected response value
    });
    } catch (error) {
    console.error(error)
}
